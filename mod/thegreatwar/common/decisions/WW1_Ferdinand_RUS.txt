RUS_franzferdinand = {

	RUS_mobilization_mission = {

		icon = GFX_decision_generic_prepare_civil_war

		available = {
			has_global_flag = kis_serbia_refuse
			has_war = no
			NOT = { has_country_flag = russia_mobilized }
		}

		selectable_mission = yes
		days_mission_timeout = 180
		is_good = no
		fire_only_once = yes

		cancel_trigger = {
			hidden_trigger = {
				OR = {
					has_war_with = AUH
					has_war_with = SER
				}
			}
		}

		complete_effect = {
			add_manpower=100000
			if = { 
				limit = { 
					has_idea = neutrality_idea 
				}
				remove_ideas=neutrality_idea
			}
			add_political_power=100
			add_war_support = 0.2
			add_stability = -0.05
			hidden_effect = {
				set_country_flag=russia_mobilized
				add_named_threat={
					threat=2
					name=ww1_franzferdinand.4.t
				}
				set_global_flag = dual_alliance_dec
				set_global_flag = kis_russia_partial_mobilization
			}
		}

		timeout_effect = {
			hidden_effect = {
				set_country_flag=russia_mobilized
				set_global_flag = dual_alliance_dec
			}
			add_war_support = -0.05
			add_stability = 0.05
			add_manpower=-15000
			add_political_power=-100
		}

		ai_will_do = {
			factor = 200
		}
	}
	
	RUS_enter_war_mission = {

		icon = GFX_decision_generic_prepare_civil_war

		available = {
			has_country_flag = russia_mobilized
		}
		
		visible = {
			has_country_flag = russia_mobilized
		}
		
		cancel_trigger = {
			hidden_trigger = {
				OR = {
					has_war_with = AUH
					has_war_with = SER
				}
			}
		}
		
		selectable_mission = yes
		days_mission_timeout = 90
		fire_only_once = yes

		is_good = no

		complete_effect = {
			add_to_war={
				targeted_alliance=SER
				enemy=AUH
			}
			set_global_flag = sp_dec # schlieffen plan decision
		}

		timeout_effect = {
			add_war_support = -0.2
			add_stability = 0.1
			add_opinion_modifier = {
				target = ENG
				modifier = medium_decrease
			}
			add_opinion_modifier = {
				target = FRA
				modifier = medium_decrease
			}
			add_opinion_modifier = {
				target = GER
				modifier = large_increase
			}
			add_opinion_modifier = {
				target = ITA
				modifier = small_increase
			}
			add_opinion_modifier = {
				target = TUR
				modifier = medium_increase
			}
			FRA = { remove_from_faction = RUS remove_from_faction = MON }
		}

		ai_will_do = {
			factor = 100
		}
	}
}