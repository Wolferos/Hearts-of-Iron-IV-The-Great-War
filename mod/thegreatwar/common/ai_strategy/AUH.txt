austro_hungarian_homeland_defense = {
	enable = {
		original_tag = AUH
		OR = {
			owns_state = 4
			owns_state = 152
			owns_state = 153
			owns_state = 39
			owns_state = 850
			owns_state = 102
			owns_state = 736
		}
		OR = {
			has_war_with = ITA
			has_war_with = GER
		}
	}
	abort = {
		AND = {
			NOT = { has_war_with = ITA }
			NOT = { has_war_with = GER }
		}
	}

	ai_strategy = {
		type = area_priority
		id = italy
		value = 75
	}
	ai_strategy = {
		type = area_priority
		id = greater_balkans
		value = 25
	}
}

austro_hungarian_battles_of_the_isonzo_river = {
	enable = {
		original_tag = AUH
		is_historical_focus_on = yes
		has_global_flag = kis_franzferdinand_murdered
		date > 1915.05.01
		has_war_with = ITA
		OR = {
			is_in_faction_with = GER
			is_in_faction_with = RUS
		}
	}
	abort = {
		OR = {
			NOT = { has_war_with = ITA }
			date > 1917.11.01
		}
	}

	ai_strategy = {
		type = area_priority
		id = italy
		value = 75
	}
	ai_strategy = {
		type = area_priority
		id = greater_balkans
		value = 75
	}
	
	ai_strategy = {
		type = invade
		id = ITA
		value = -25	### Austria-Hungary on the defensive initially.
	}
}

austro_hungarian_schlieffen_started_defend_east = {
	enable = {
		original_tag = AUH
		is_in_faction_with = GER
		has_war_with = RUS
		OR = {
			has_war_with = BEL
			AND = {
				has_war_with = FRA
				BEL = { exists = no }
			}
		}
		date > 1914.07.01
	}
	abort = {
		OR = {
			NOT = { has_war_with = RUS }
			date > 1915.05.01
		}
	}

	ai_strategy = {
		type = area_priority
		id = greater_balkans
		value = 40
	}
	ai_strategy = {
		type = area_priority
		id = hoi4tgw_poland
		value = 40
	}
	ai_strategy = {
		type = area_priority
		id = italy
		value = 40
	}
	
	ai_strategy = {
		type = invade
		id = RUS
		value = -50
	}
}
